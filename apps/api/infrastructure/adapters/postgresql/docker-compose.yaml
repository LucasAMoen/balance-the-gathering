services:
  db:
    container_name: "balance_the_gathering_db"
    image: postgres:latest

    restart: always
    env_file: 
      - ".env"
    ports:
      - 5432:5432

  adminer:
      image: adminer
      restart: always
      ports:
        - 8080:8080